/** @type {import('next').NextConfig} */
// Default output — required for Vercel serverless + Pages API routes.
// Do not use `output: "standalone"` here; that mode is for Docker/self-hosted images.
const nextConfig = {
  reactStrictMode: true
};

module.exports = nextConfig;

